NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never exposed to the browser. From Supabase: Project Settings > API > service_role.
SUPABASE_SERVICE_ROLE_KEY=

# Server-only — the single shared admin passcode (ARCHITECTURE §2). Pick anything reasonable.
ADMIN_PASSCODE=
